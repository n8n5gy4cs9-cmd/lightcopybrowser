
Build an Android app called LightCopy Browser.

Objective: An ultra-lightweight, super-fast, privacy-focused Android browser built specifically for developers, researchers and power users.

Core Principle: Keep it VERY lightweight - instant launch, minimal memory, no bloat, no tracking, under 15MB. Offline-first. Everything stays on device.

MUST-HAVE FEATURES - Do not remove:

Android browser that can browse any website normally.
One-tap COPY FULL SOURCE CODE of current page (original HTML).
One-tap COPY RENDERED TEXT of current page (only visible text, no HTML tags, clean for reading/AI).
Built-in JS Console that captures FULL JS CONSOLE LOGS and has a filter to show ONLY ERRORS. Ability to copy all logs or copy only errors, clear and export.
FULLSCREEN MODE TOGGLE to hide all browser UI and system bars to see more content. When in fullscreen, always show a small floating EXIT FULLSCREEN button. Default position is bottom middle, semi-transparent. In Settings user can change its location to ANY corner: bottom-middle, bottom-left, bottom-right, top-left, top-right, top-middle, or free drag to save custom position. Also setting for button size, opacity and auto-hide delay.
PRO FEATURES:
6. Copy tools: Copy rendered DOM (final HTML after JS), copy selected element's HTML, copy page URL, title, all links on page, copy all images URLs, copy as Markdown.
7. Element Inspector: Tap any element on the page to see its tag, id, class, and copy its HTML/CSS selector.
8. Page Info Panel: Title, URL, SSL status, load time, viewport size, all meta tags, Open Graph, favicon, cookies viewer, LocalStorage & SessionStorage viewer with copy/clear.
9. Network Log: Simple list of all requests made by the page (URL, method, status code).
10. Dev Toggles: Turn JavaScript ON/OFF per tab, Ad-Block ON/OFF, User-Agent switcher (Mobile/Desktop/Custom).
11. Browser Essentials: Multiple tabs, Incognito mode, Find in page, Reader Mode for clean text, Dark Mode, Full-page screenshot, Save page as HTML or TXT.
12. Quick Actions: Bottom toolbar with Source / Text / Console / Info buttons. Floating button for console. Long-press on page to inspect/copy element. Instant "Copied!" feedback.
13. Utilities: Built-in QR scanner to open URLs, Share menu, Download manager, Lightweight History & Bookmarks (local only).
14. DEVELOPER DREAM SETTINGS PAGE: This must be extensive:

Fullscreen button: position, size, opacity, auto-hide
Appearance: Light/Dark/AMOLED theme, address bar top/bottom, source viewer font size, line numbers, word-wrap, syntax highlight theme
Console: Show timestamps, preserve log on navigation, auto-clear, log level filter
Browsing: Default search engine, default tab mode (normal/incognito), keep screen on, desktop mode by default, block images to save data
Copy defaults: What format to copy by default
Privacy: Clear on exit (history/cache/cookies/storage), Do Not Track
Advanced: Custom User-Agent list editor, Custom CSS injection, Custom JS injection on page load, Vibrate on copy, Export/Import all settings, Change default download folder
UI/UX: Minimal, Material You design. Bottom address bar for one-hand use. No unnecessary screens. All copy actions should take 1 tap.

Build a complete, working app with polished UI. Let me decide the final design details.